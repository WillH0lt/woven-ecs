{
  "private": true,
  "type": "module",
  "name": "@woven-ecs/monorepo",
  "description": "A multi-threaded Entity Component System (ECS) Framework for TypeScript",
  "repository": "https://github.com/WillH0lt/woven-ecs",
  "author": {
    "name": "Will Whiteneck",
    "email": "will@william.land"
  },
  "license": "MIT",
  "engines": {
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.13.0",
  "scripts": {
    "build": "turbo run build",
    "build:watch": "turbo run build:watch",
    "check:exports": "turbo run check:exports",
    "clean": "turbo run clean",
    "lint:ci": "biome ci",
    "lint": "biome check --fix --unsafe",
    "test": "pnpm -r --if-present test",
    "test:watch": "pnpm -r --if-present test:watch",
    "prepare": "husky"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@biomejs/biome": "^2.3.14",
    "@microsoft/api-extractor": "^7.56.3",
    "@types/node": "24.10.0",
    "husky": "^9.1.7",
    "jsdom": "^27.3.0",
    "lint-staged": "^16.2.6",
    "resize-observer-polyfill": "^1.5.1",
    "rimraf": "^6.1.0",
    "tsup": "^8.5.0",
    "turbo": "^2.6.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.7"
  },
  "lint-staged": {
    "*.{js,ts,cjs,mjs,md,json,jsonc}": "biome ci --no-errors-on-unmatched"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
